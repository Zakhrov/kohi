project(KohiTestbed)
include_directories(include)
include_directories(${CMAKE_SOURCE_DIR}/engine/include)
add_executable(${PROJECT_NAME})
add_subdirectory(src)
# add_dependencies(${PROJECT_NAME} KohiAssets)