project(KohiTests)
include_directories(${CMAKE_SOURCE_DIR}/engine/include)
add_executable(${PROJECT_NAME})
add_subdirectory(src)
target_link_libraries(${PROJECT_NAME} KohiEngine)
target_include_directories(${PROJECT_NAME} PUBLIC include)